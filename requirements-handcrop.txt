# Optional: MediaPipe hand detection + crop (src/handcrop.py).
#
# This is an optional enhancement — the core training/eval/inference pipeline
# imports it lazily and runs fine without it. Install only when you want the
# hand-crop preprocessing step (closes the benchmark→real-world gap):
#
#     uv pip install -r requirements-handcrop.txt
#
# The browser does its own hand detection via @mediapipe/tasks-vision (in web/).
mediapipe>=0.10.35,<0.11
